[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --worker-class=gthread --threads=8 --timeout=300 --bind=0.0.0.0:$PORT"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
